# TASK-019 offline writable Set List evidence

This package records deterministic software evidence for the first writable V2
browser slice. Publication and browser authoring remain independently gated and
disabled in the tracked production service.

`writable-set-list-evidence.json` is generated by exercising the production
TypeScript domain and IndexedDB v3 repository with fixed identities/timestamps.
It proves duplicate occurrence identity, targeted notes, reorder semantics,
forward-revision undo, fresh duplicate identities, atomic draft/revision/outbox
persistence, pending-envelope coalescing, byte-stable retry, and hashed
export/restore.

The checked evidence SHA-256 is
`39d6b8443391a6933330d20880ec006b5948cb35e229c093ff12e96eb6e64a33`.

Verify with:

```sh
make v2-writable-set-list-check
```

The full acceptance also includes the web Vitest suite and Go race tests for
baseline bootstrap, current/publication mappings, replay-before-reservation,
and the disabled-by-default runtime gates.
