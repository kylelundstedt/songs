{
  "name": "songs-v2",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @songs-v2/read-model && npm run build --workspace @songs-v2/bootstrap-api",
    "check": "npm run check --workspace @songs-v2/read-model && npm run check --workspace @songs-v2/bootstrap-api",
    "test": "npm run test --workspace @songs-v2/read-model && npm run test --workspace @songs-v2/bootstrap-api",
    "fixtures": "npm run fixtures --workspace @songs-v2/read-model && npm run fixtures --workspace @songs-v2/bootstrap-api",
    "fixtures:generate": "npm run fixtures:generate --workspace @songs-v2/read-model && npm run fixtures:generate --workspace @songs-v2/bootstrap-api"
  }
}
